version: '2'

services:
    web:
        image: nginx:latest
        ports:
            - "8080:80"
        volumes:
            - ./mysql-test:/code
            - ./dockerised-php/site.conf:/etc/nginx/conf.d/default.conf
        networks:
            - code-network
    php:
        image: php-mysql
        volumes:
            - ./mysql-test:/code
        networks:
            - code-network

networks:
    code-network:
        driver: bridge
